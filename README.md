# CureData · Dave’s Data (shared)

**Click:** open the live site link Brian sent (this folder’s `index.html` is the app).  
**Questions:** edit `QUESTIONS.md` in this repo (or comment — Brian mirrors).

When Brian updates CureData architecture, agents sync this map. Refresh the link for latest.
